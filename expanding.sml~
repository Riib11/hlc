structure Expanding = struct

open Utilities;
open Grammar;

fun expand (expr : expression) : expression =
    case expr of
	Binding (x1, e1, e2) => Application ((x1, e2), e1)
      | expr => expr

end
